import type { ToolManifest } from "@/lib/registry";

const manifest: ToolManifest = {
  slug: "serp-landscape",
  name: "SERP Landscape",
  description:
    "Where a site actually ranks — position-bucket distribution plus the top queries by impressions.",
  category: "SEO Research",
  owner: "FirstPage Team",
  status: "active",
  icon: "🗺️",
};

export default manifest;
