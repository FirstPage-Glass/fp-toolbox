import type { ToolManifest } from "@/lib/registry";

const manifest: ToolManifest = {
  slug: "url-inspector",
  name: "URL Inspector",
  description:
    "One-stop URL diagnosis: Google index status, canonical, mobile usability and rich results — plus the PageSpeed score.",
  category: "SEO Technical",
  owner: "FirstPage Team",
  status: "active",
  icon: "🔬",
};

export default manifest;
