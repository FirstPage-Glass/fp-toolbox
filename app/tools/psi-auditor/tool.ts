import type { ToolManifest } from "@/lib/registry";

const manifest: ToolManifest = {
  slug: "psi-auditor",
  name: "PageSpeed Auditor",
  description:
    "Run a PageSpeed Insights audit on any URL — performance score, LCP and CLS for a quick mobile health check.",
  category: "SEO Technical",
  owner: "FirstPage Team",
  status: "active",
  icon: "⚡",
};

export default manifest;
