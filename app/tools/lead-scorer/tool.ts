import type { ToolManifest } from "@/lib/registry";

const manifest: ToolManifest = {
  slug: "lead-scorer",
  name: "Lead Scorer",
  description:
    "Score the newest HubSpot leads hot/warm/cold — email reputation, mailbox type and website signals, with the reasons.",
  category: "Sales",
  owner: "FirstPage Team",
  status: "active",
  icon: "🔥",
};

export default manifest;
