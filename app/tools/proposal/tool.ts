import type { ToolManifest } from "@/lib/registry";

const manifest: ToolManifest = {
  slug: "proposal",
  name: "Proposal Generator",
  description: "Draft a full proposal from the same client brief — one pipeline, proposal template.",
  category: "Sales",
  owner: "Glass Chan",
  status: "active",
  icon: "📝",
};

export default manifest;
