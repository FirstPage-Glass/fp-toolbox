import type { ToolManifest } from "@/lib/registry";

const manifest: ToolManifest = {
  slug: "keyword-gap",
  name: "Keyword Gap Analyzer",
  description:
    "Two domains side by side — the keywords one ranks for that the other is missing, with volumes. Classic opportunity mining.",
  category: "SEO Research",
  owner: "FirstPage Team",
  status: "active",
  icon: "🕳️",
};

export default manifest;
