import type { ToolManifest } from "@/lib/registry";

const manifest: ToolManifest = {
  slug: "cwv-batch",
  name: "Core Web Vitals Batch",
  description:
    "Audit up to 10 URLs at once — performance score, LCP and CLS side by side to spot the slow pages.",
  category: "SEO Technical",
  owner: "FirstPage Team",
  status: "active",
  icon: "🧪",
};

export default manifest;
