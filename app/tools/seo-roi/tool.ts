import type { ToolManifest } from "@/lib/registry";

const manifest: ToolManifest = {
  slug: "seo-roi",
  name: "SEO ROI Estimator",
  description:
    "Turn keyword volumes and your average deal value into a defensible traffic / leads / revenue estimate — with the assumptions spelled out.",
  category: "Sales",
  owner: "FirstPage Team",
  status: "active",
  icon: "💹",
};

export default manifest;
