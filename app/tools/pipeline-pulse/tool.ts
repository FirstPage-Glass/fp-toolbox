import type { ToolManifest } from "@/lib/registry";

const manifest: ToolManifest = {
  slug: "pipeline-pulse",
  name: "Pipeline Pulse",
  description:
    "The sales pipeline in one view — new deals, open pipeline value, closed-won revenue and the per-owner leaderboard.",
  category: "Sales",
  owner: "FirstPage Team",
  status: "active",
  icon: "💰",
};

export default manifest;
